# Square Cloud — plano B quando as variáveis do painel não funcionam
# Copie para .env.squarecloud e preencha. Faça commit para o deploy usar.
# Use APENAS em repositório PRIVADO (nunca suba senhas em repo público).

DATABASE_URL=postgresql://usuario:senha@host:porta/neondb?sslmode=require
JWT_SECRET=uma_string_longa_com_pelo_menos_32_caracteres
